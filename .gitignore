node_modules/
*.log
.DS_Store
.data/
build-pkg/

# Build inputs, far too large to ship.
native/target/
# The old C++ addon's 821 MB vendored faiss/tenann tree. Nothing builds against
# it any more; it is kept on disk only so the migration can be reverted.
vendor-tenann/

# NOTE: dist/ and native/starmemory_native.node are deliberately committed. A
# marketplace install is a git clone, so anything not in git is absent at runtime.
